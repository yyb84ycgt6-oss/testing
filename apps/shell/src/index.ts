// Command Center App Shell — Slice 1
// Mounts shared platform; module routes loaded dynamically.
export { hasRole } from '@cc/shared';
export { evaluate } from '@cc/shared';
export { record, readLog } from '@cc/shared';
export { createWorkflow } from '@cc/shared';
